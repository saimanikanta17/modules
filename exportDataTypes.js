// Default Exports
//  We cannot export boolean, number, string, null, undefined, objects, and arrays while defining.
const dataType = {
  x: 1,
  y: 3,
};
module.exports = dataType; // exporting the module
